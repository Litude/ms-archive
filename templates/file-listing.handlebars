<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Index of /{{requestPath}}</title>
  <style>
    body { font-family: sans-serif; }
    h1 { font-size: 1.2em; }
    th, td { padding: 4px 8px; border-bottom: 1px solid #ddd; }
    td a { text-decoration: none; color: #06c; }
    td a:hover { text-decoration: underline; }
  </style>
</head>
<body>
  <h1>Index of /{{requestPath}}</h1>
    <table>
        <tr>
            <th><img src="/static/fileicons/blank.gif" alt="[ICO]"></th>
            <th>Name</th>
            <th>Last modified</th>
            <th>Size</th>
            <th>Start date</th>
            <th>End date</th>
            <th>Status</th>
            <th>Description</th>
        </tr>
        <tr><th colspan="8"><hr></th></tr>
        {{#each files}}
        <tr>
            <td><img src="/static/fileicons/{{icon}}" alt="[{{iconAlt}}]"></td>
            <td><a href="{{href}}">{{name}}</a></td>
            <td>{{lastModified}}</td>
            <td align="right">{{size}}</td>
            <td align="center">{{firstDate}}</td>
            <td align="center">{{lastDate}}</td>
            <td>{{status}}</td>
            <td>{{{description}}}</td>
        </tr>
        {{/each}}
        <tr><th colspan="8"><hr></th></tr>
    </table>
</body>
</html>
